import { registerFeature } from '../../core/navigation.registry';

registerFeature({
  id: 'documents',
  name: 'Documentos',
  navigation: [{ label: 'Documentos', path: '/documents', icon: '📁' }],
  routes: [],
});

import { registerFeature } from '../../core/navigation.registry';

registerFeature({
  id: 'reports',
  name: 'Reportes',
  navigation: [{ label: 'Reportes', path: '/reports', icon: '📈' }],
  routes: [],
});
